apiVersion: batch/v1
kind: Job
metadata:
  name: placeholder
  namespace: placeholder
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: job
          image: busybox:latest
          command: ["echo"]
          args: ["hello"]
